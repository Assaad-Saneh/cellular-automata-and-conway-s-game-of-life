To dwonload the project requirements run the following command:
> pip install -r requirements.txt

To run the project run the following command:
> python app.py

you can also check the .ipynb file in the jupyter notebook files